{
  "name": "@fotw/pro",
  "version": "0.1.0",
  "type": "module",
  "license": "BUSL-1.1",
  "description": "FOTW² Pro — the protected cross-wrench correlation spine and premium bridges. Source-available, not for resale. See LICENSE.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@help-me-comms/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
