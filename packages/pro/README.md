# @fotw/pro — the protected spine

This is the **Pro layer** of FOTW². It contains the cross-wrench **correlation
spine** (threading community signal → mod cause → dev fix → creator impact into
one story) and premium wrench bridges.

**License:** BUSL-1.1 (source-available, not for resale) — *not* MIT. See
[`LICENSE`](./LICENSE) and `docs/STRATEGY.md` §3. The rest of the suite
(`@help-me-comms/core`, `@help-me-comms/adapters`, the extension shells) is MIT.

The `WrenchBridge` **contract** lives in `@help-me-comms/core` (MIT) on purpose —
forkers build their own bridges against it. The **spine that correlates across
bridges** lives here, gated.
