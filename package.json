{
  "name": "help-me-comms-mcp-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Codex-ready scaffold for a communication-focused MCP and companion app/extensions for game devs and modders.",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "check:no-sensitive-fixtures": "node scripts/check-no-sensitive-fixtures.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
